


/* 
pub fn install_device_hooks(com: *mut c_void) {
    let hook_map = make_hook_map!(
        (5, create_texture2d),
        (27, create_deferred_context),
    );
    install_hooks(com, &hook_map);
}
*/

